# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment files - never commit secrets
.env
.env.local
.env.production
.env.*.local

# Testimony curation decisions - editorial working state, never publish
.testimony-decisions.json

# Local staging inbox: working COPY of the chronological testimony corpus
# (originals live outside this repo). UNPUBLISHED material must never enter
# this public repo's tracking — testimonies enter the repo ONLY when the
# curation console publishes them into testimonies/<Set>/.
/Chronological Testimonies/

# Next.js (Phase 2 port)
/.next/
next-env.d.ts
